name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: "macOS-10.15"

    steps:
    - uses: actions/checkout@v2
    - name: Build
      run: xcodebuild -scheme TravelokaCinema test -destination "platform=iOS Simulator,name=iPhone 12 mini"
